import React, { Component } from "react";
import { connect } from "react-redux";

class DriversTripsNew extends Component {
  render() {
    return <div>Hello world!</div>;
  }
}

export default connect()(DriversTripsNew);
